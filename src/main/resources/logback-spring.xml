<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Use different log directory for tests vs production -->
    <springProfile name="test">
        <property name="LOG_DIR" value="target/logs"/>
    </springProfile>

    <springProfile name="!test">
        <property name="LOG_DIR" value="/var/log/csye6225"/>
    </springProfile>

    <!-- Console Appender (plain text for local development readability) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Application Log File (JSON format) -->
    <appender name="APP" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/app.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <maxHistory>7</maxHistory>
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- Add custom fields -->
            <customFields>{"application":"csye6225"}</customFields>

            <!-- Include MDC fields -->
            <includeMdcKeyName>reqId</includeMdcKeyName>
            <includeMdcKeyName>user</includeMdcKeyName>
            <includeMdcKeyName>method</includeMdcKeyName>
            <includeMdcKeyName>path</includeMdcKeyName>
            <includeMdcKeyName>status</includeMdcKeyName>
            <includeMdcKeyName>durMs</includeMdcKeyName>

            <!-- Include caller data (file, line, function) -->
            <includeCallerData>true</includeCallerData>

            <!-- Shorten field names for cleaner output -->
            <shortenedLoggerNameLength>36</shortenedLoggerNameLength>
        </encoder>
    </appender>

    <!-- Error Log File (JSON format, WARN and above) -->
    <appender name="ERRORS" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/app-error.log</file>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>WARN</level>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/app-error.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <maxHistory>14</maxHistory>
            <totalSizeCap>2GB</totalSizeCap>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"application":"csye6225"}</customFields>
            <includeMdcKeyName>reqId</includeMdcKeyName>
            <includeMdcKeyName>user</includeMdcKeyName>
            <includeMdcKeyName>path</includeMdcKeyName>
            <includeCallerData>true</includeCallerData>
            <includeStackTrace>true</includeStackTrace>
        </encoder>
    </appender>

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="APP"/>
        <appender-ref ref="ERRORS"/>
    </root>

    <!-- Application-specific logging levels -->
    <logger name="com.example.healthcheckapi" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.springframework.security" level="INFO"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="INFO"/>

</configuration>